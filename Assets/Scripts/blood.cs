using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;


public class blood : MonoBehaviour
{
    // public GameObject Boss = GameObject.FindGameObjectWithTag("Boss");
    // private static int MAX_HEALTH = 10;
    // private int health = Boss.GetComponent<BossController>().health;

    // Start is called before the first frame update
    void Start()
    {
        // Boss = GameObject.FindGameObjectWithTag("Boss");


    }


    // Update is called once per frame
    void Update()
    {
        // GetComponent<Slider>().value = (MAX_HEALTH - health) + GetComponent<Slider>().value;
    }
}
